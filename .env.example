# AI Multi-Agent CLI Configuration
# Copy this file to .env and update the values according to your setup

# ================= Pipeline Configuration =================
REVIEW_LOOP_ENABLED=true
REVIEW_LOOP_MAX_LOOPS=3

# ================= Planner Agent (Gemini) =================
PLANNER_COMMAND=gemini
PLANNER_ARGS=
PLANNER_TIMEOUT_MS=3600000
PLANNER_PROVIDER=google
PLANNER_MODEL=gemini-2.0-pro

# ================= Implementer Agent (Cursor) =================
IMPLEMENTER_COMMAND=cursor-agent
IMPLEMENTER_ARGS=
IMPLEMENTER_TIMEOUT_MS=3600000
IMPLEMENTER_PROVIDER=cursor
IMPLEMENTER_MODEL=gpt-4.1

# ================= Tester Agent =================
TESTER_COMMAND=cursor-agent
TESTER_ARGS=
TESTER_TIMEOUT_MS=3600000
TESTER_PROVIDER=cursor
TESTER_MODEL=gpt-4.1

# ================= Reviewer Agent (Codex) =================
REVIEWER_COMMAND=codex
REVIEWER_ARGS=
REVIEWER_TIMEOUT_MS=3600000
REVIEWER_PROVIDER=openai
REVIEWER_MODEL=gpt-4.1

# ================= Path Configuration =================
# TARGET_PROJECT_ROOT: Directory where agents will generate/create code
# 
# IMPORTANT: 
# - This is where the code will be generated (src/, tests/, etc.)
# - Leave empty or comment out to use orchestrator directory (same as current directory)
# - Status file (ai_status.json) and docs are always stored in orchestrator directory
# 
# Example: TARGET_PROJECT_ROOT=/Users/username/project/my-project
# 
# To generate code in current directory (orchestrator location):
# TARGET_PROJECT_ROOT=
# 
# To generate code in another project:
# TARGET_PROJECT_ROOT=/path/to/target/project
TARGET_PROJECT_ROOT=

# AI_ORCH_DIR: Directory name for storing orchestration files
# Default: .ai-orch
AI_ORCH_DIR=.ai-orch

# ================= Notes =================
# - COMMAND: The CLI command name (must be in PATH)
# - ARGS: Space-separated arguments (leave empty for none)
#   Example: ARGS="--model gpt-4 --verbose"
# - TIMEOUT_MS: Timeout in milliseconds (default: 3600000 = 1 hour)
# - PROVIDER: Provider name (informational only)
# - MODEL: Model name (informational only)

# DOCS_IN_TARGET_PROJECT: Store docs in target project instead of orchestrator directory
# - false (default): Docs stored in orchestrator directory
# - true: Docs stored in target project (docs/ folder will be created there)
DOCS_IN_TARGET_PROJECT=false
